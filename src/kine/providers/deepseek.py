"""Pending DeepSeek provider implementation.

DeepSeek does not currently have an official PyPI package, so this adapter
remains a placeholder. When a stable SDK or REST spec is available, it should
implement the domain provider protocol using `httpx` or the official client.
"""
